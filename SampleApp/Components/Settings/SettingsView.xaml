<UserControl x:Class="SampleApp.Components.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SampleApp.Components.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:SettingsViewModel}" 
             d:DesignHeight="202.796" 
             d:DesignWidth="321.875"
             >
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Background="DarkGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Background" Value="#DDDDDD"/>
                    <Setter Property="Margin" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="4,2,4,2"/>
                </Style>
            </Grid.Resources>

            <TextBlock Text="Application name" Margin="1,1,1,1"/>
            <TextBlock Grid.Column="1" Margin="0,1,1,1" Text="{Binding ApplicationName}"/>

            <TextBlock Grid.Row="1" Text="Content root path" Margin="1,0,1,1"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,1,1" Text="{Binding ContentRootPath}"/>

            <TextBlock Grid.Row="2" Text="Environment name" Margin="1,0,1,1"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding EnvironmentName}"/>

            <TextBlock Grid.Row="3" Text="Providers" Margin="1,0,1,1"/>
            <ListBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Providers}"/>
            
        </Grid>

    </Grid>
</UserControl>
