<UserControl x:Class="SampleApp.Components.UI.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="131.25" 
             d:DesignWidth="246.875">
    
    <Border Style="{DynamicResource BorderTop_Default}">
        
        <TextBlock Text="{Binding Error,FallbackValue=''}"
                Margin="{DynamicResource Margin_Default}"
                Style="{DynamicResource ErrorText}" />
        
    </Border>
</UserControl>
